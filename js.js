/*function calcularIdade(idade1, idade2) {
  let resultado = idade1 + idade2;
  return resultado >= 99 ? "voce tem" : "voce n tem";
}

console.log(calcularIdade(99,)); */

const calcularIdade = (idade) => `a sua idade é ${idade}`;
console.log(calcularIdade(89));
